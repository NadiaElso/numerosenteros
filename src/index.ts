let primernumero = Number(prompt("Ingrese un  primer número entero"));
let segundonumero = Number(prompt("ingrese un  segundo número entero"));
let numeroactual, suma: number;
let numeroactual = primernumero;
suma = 0;

while (numeroactual <= segundonumero) {
  suma = suma + numeroactual;
  actual++;
}
console.log("El resultado de la suma es:", suma);
